import numpy as np
from sympy import *
import matplotlib.pyplot as plt
from coeffs import *

#if using termux
import subprocess
import shlex

a1 = -np.sqrt(3)
b1 = np.sqrt(2)
a2 = 2 * np.sqrt(3)
b2 = -1

p = np.array([[a1, -b1], [b1, a1]])
q = np.array([[a2, -b2], [b2, a2]])
result = p.dot(q)

print(result)