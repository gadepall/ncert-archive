import numpy as np

Y = np.array([[3,2], [1,4]])

#initialising the value of X to verify the problem

X = np.array([[-1,-1],[-2,-1]])

#calculating the matrix 2X+Y 
ans = 2*X + Y

print("2X + Y = ", ans)
