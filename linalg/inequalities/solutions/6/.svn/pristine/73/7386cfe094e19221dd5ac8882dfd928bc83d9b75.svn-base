import numpy as np 
import matplotlib.pyplot as plt
from coeffs import *
import subprocess
import shlex

A = np.array([-4,2])
C = np.array([3,-2])
D = np.array([2,3])

area = (1/2)*np.linalg.norm(np.cross(C-A,D-A))

print("Area of triangle ACD: ",area)